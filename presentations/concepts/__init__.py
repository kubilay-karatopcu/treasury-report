"""Phase 7 — Concept Foundation.

A concept-aware filter layer between the user/LLM and the underlying tables.
See ``docs/PHASE_7_SPEC.md`` for the authoritative design.

Sub-phase 7.a delivers the concept registry + schema + migration + API.
Later sub-phases add column bindings (7.b), the filter compiler (7.b),
binding inference (7.c) and user-scoped concepts (7.d).
"""
