table: DIM_PRODUCT
schema: LOOKUP
description: |
  Ürün boyut tablosu — kod, ad, kategori. Mevduat/kredi raporlarının
  ürün gruplaması bu tabloya bakar.

columns:
  PRODUCT_CODE:
    type: VARCHAR2(10)
    description: "Ürün kodu"
    filterable: true
    filter_role: dimension
    suggested_variable: products
    suggested_semantic_tag: product_group

  PRODUCT_NAME:
    type: VARCHAR2(80)
    description: "Ürün adı"
    filterable: false

  CATEGORY:
    type: VARCHAR2(30)
    description: "Üst kategori (MEVDUAT, KREDI, FX, MENKUL)"
    filterable: true
    filter_role: dimension
    suggested_variable: categories
    suggested_semantic_tag: product_group
    distinct_values_sample: [MEVDUAT, KREDI, FX, MENKUL, REPO]
    distinct_values_sampled_at: 2026-05-21T03:00:00Z
